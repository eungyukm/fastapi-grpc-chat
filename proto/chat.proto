syntax = "proto3";

service ChatService {
  rpc SendMessage(ChatMessage) returns (Empty);
  rpc StreamMessages(Empty) returns (stream ChatMessage);
}

message ChatMessage {
  string username = 1;
  string message = 2;
}

message Empty {
}